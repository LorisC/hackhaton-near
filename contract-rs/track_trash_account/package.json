{
  "name": "track_trash_account",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf res && mkdir res && RUSTFLAGS='-C link-arg=-s --verbose' cargo build --target wasm32-unknown-unknown --release && cp target/wasm32-unknown-unknown/release/track_trash_account.wasm res/",
    "test": "cargo test --package track_trash_account --lib"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
